The label of input elements whose type is button comes from the tag's value attribute. On the other hand, the label of a button element is taken from the element's content and therefore offers richer rendering possibilities because it can contain HTML markup. 
